
To use the template:

+ Create a new folder "designs" or copy the example folder into your project; 
	set it up as a source folder (DO NOT add to build.properties)
+ Create package folders in designs/ for each API package in src/. 
+ Copy the "package.html" template to the package folder in designs/, along with any images you use.
+ Copy the files at the root of the designs/ folder template to the root of your new 
	designs/ source folder.
	